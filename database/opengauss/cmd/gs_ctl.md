# gs_ctl

```shell
启动数据库
gs_ctl start -D /path/to/data/directory

停止数据库
gs_ctl stop -D /path/to/data/directory -m fast

重启数据库
gs_ctl restart -D /path/to/data/directory

查询数据库状态
gs_ctl status -D /path/to/data/directory
```
