# 位运算

- xxx & -2
```shell
& -2 的作用是：
-2 的二进制表示：在补码系统中，-2 的二进制是所有位都为1，除了最低位为0
32位系统：0xFFFFFFFE （11111111111111111111111111111110）
64位系统：0xFFFFFFFFFFFFFFFE
按位与操作：x & -2 会将 x 的最低位强制设为0
如果 x 是奇数：x & -2 = x - 1（向下取偶）
如果 x 是偶数：x & -2 = x（保持不变）
```
