# bash

bash ! 是特殊字符，用来执行历史命令

- 查看环境变量
```shell
echo $-
himBHs

h - hashall
i - interactive-comments
m - monitor
B - braceexpand
H - history
s - 暂未知
c - 暂未知
```



- set 命令
```shell

查看： set -o
设置： set -N 或者 set -o Nx
取消： set +N
```

- -/+ 作用
```shell
-：设置值
+：取消设置值
```

